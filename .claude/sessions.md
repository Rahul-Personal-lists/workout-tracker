# Session log

(latest first; only the most recent 3 sessions are kept)

## 2026-07-08 — Startup & navigation speed (#115)

- On `feat/startup-speed` ([#115](https://github.com/Rahul-Personal-lists/workout-tracker/pull/115)): cold opens and screen switches made fast — **`staleTimes {dynamic:30, static:300}`** (pill/tab re-visits ≤30 s serve from the client cache; own mutations stay fresh via `revalidatePath`), **query-waterfall collapse** (`/program` ~6→~3 roundtrips: reap joined wave 1 — it touches only sessions/logs, and the session-reading wave 2 **must stay after it**; `getNextWorkout` reads in parallel; `/workout` session∥program; `/progress` single wave), **7 new `loading.tsx`** (history ×2, library ×3, exercises ×2), **`NavPending`** (`useLinkStatus`, 150 ms-delay pulse) on day pills/range tabs/month grid, **signed-URL memo** [src/lib/signed-url-cache.ts](src/lib/signed-url-cache.ts) (injected signer so plain-tsx testable, [smoke](scripts/smoke-signed-url-cache.ts) 10/10; `signCustomVideoUrl` deliberately uncached — fresh-after-403; photo-sign failures now degrade to `""` instead of throwing), **lazy Recharts ×3**, preconnects to raw.githubusercontent.com + Supabase origin.
- **Numbers** (local `next start` ×2 — baseline origin/main :3001 vs branch :3000 — test account, Playwright; us-east DB from Vancouver overstates absolute vs prod-on-Vercel): `/program` load median **950→490 ms**; day-pill hop 553–584→331–381 ms; **re-hop ≤30 s 544→66–82 ms**; history tap: **skeleton at 131 ms** (was frozen ~400 ms). Zero console errors. Tabs already fast (bottom-nav `prefetch` full-prefetch, 5-min TTL) — the wins are searchParams navs + post-mutation refetches.
- **Rejected (don't re-pitch):** Cache Components/PPR (modest gain on 100 % per-user data; Expo rewrite supersedes), SW HTML caching (stale-chunk 404 hazard sw.js documents), TanStack Query, theme-cookie restructure. **Rahul's dashboard checklist** (Fluid Compute, iad1 region, Deployment Protection OFF) in NEXT-SESSION.
- **Verification:** `tsc`/`lint` (0 errors; 2 pre-existing `progression.ts` warnings)/`build` green, smokes 10/10 + 6/6 + 9/9, live before/after per above. Leftover: empty test session `004e295b` on claude-test (created for the history-tap measurement; delete was permission-blocked — Rahul's call). Built brainstorm → [spec](docs/superpowers/specs/2026-07-08-startup-speed-design.md) → [plan](docs/superpowers/plans/2026-07-08-startup-speed.md) → inline execution.

## 2026-07-08 — Android shell (Capacitor) + Expo rewrite roadmap (#114)

- On `feat/capacitor-shell` ([#114](https://github.com/Rahul-Personal-lists/workout-tracker/pull/114)): the app as a **real Android APK** without the rewrite — a **Capacitor 8 shell** in [mobile/](mobile/README.md) whose webview loads the deployed Vercel app (`server.url` → the stable alias), giving a home-screen app + **real rest-timer notifications while locked** (the two things the PWA can't do). Also wrote [docs/MOBILE_ROADMAP.md](docs/MOBILE_ROADMAP.md): the 7-phase / ~10–15-PR **Expo rewrite** plan (own repo, same Supabase DB; ~1,650 pure-logic lines carry over, 71 client components + 41 server actions rebuilt as supabase-js + TanStack Query; shared-invariants decision — duplicate vs Postgres RPCs — deliberately parked at its Phase 2; custom-exercise media stays web-only in v1). Shell = Phase 0 stopgap.
- **Web side is one bridge, zero deps:** pure [src/lib/native-shell.ts](src/lib/native-shell.ts) `planRestNotification` (+ [scripts/smoke-native-shell.ts](scripts/smoke-native-shell.ts), 9 cases) and [native-shell-bridge.tsx](src/components/native-shell-bridge.tsx) mounted once in `(app)/layout.tsx` — subscribes to the untouched rest-timer store, schedules/cancels notification id 4477 at `endsAt` (`allowWhileIdle`, channel `rest-timer`), lazy permission request, hardware-back handling. **No-ops unless `window.Capacitor.isNativePlatform()`** (the shell injects the bridge into the remote page). Gotcha: Capacitor CLI can't parse `capacitor.config.ts` without a `typescript` dep — use **`capacitor.config.json`**. Manifest carries `POST_NOTIFICATIONS` + `SCHEDULE_EXACT_ALARM`/`USE_EXACT_ALARM`.
- **No Android SDK on this machine (Rahul: don't install)** — `.github/workflows/android-shell.yml` builds the debug APK (Temurin 21, `chmod +x gradlew`, `mobile/.gitattributes` forces LF on gradlew) and uploads artifact `workout-tracker-shell-debug-apk` on `mobile/**` changes. **Blocker for first launch: Vercel Deployment Protection is ON for prod** (alias 302s to Vercel SSO — a fresh webview can't pass); Rahul must flip it off, then sideload the artifact. Device-bound gaps: permission prompt, locked-phone fire, hardware back.
- **Verification:** `tsc`/`lint` (0 errors; 2 pre-existing `progression.ts` warnings)/`build` green, smoke 9/9, live preview `/program` zero console errors (bridge no-op), **and the wiring proven end-to-end in-browser** via a stubbed `window.Capacitor` + persisted running timer: `createChannel(rest-timer)` → `cancel(4477)` → `schedule(4477, endsAt)` with the exact persisted timestamp. Built brainstorm → [spec](docs/superpowers/specs/2026-07-08-capacitor-shell-design.md) → [plan](docs/superpowers/plans/2026-07-08-capacitor-shell.md) → inline execution.

## 2026-06-29 — Day-order fix (rahul@satel.ca live data) + hardening (#113)

- **Live-data incident:** rahul@satel.ca's active "12-Week Hypertrophy" rendered days out of order (at week 8: Day 52 = Bench, Day 53 = rest-not-squat, Day 54 = incline-not-rest, …). Root cause = the program's `program_days.day_number` order had diverged from intent: `reorderDay`/`swap_day_order` swaps `day_number` but leaves the text `label` stale, and an archived empty "New day" sat wedged at `day_number` 5, leaving live numbers non-contiguous `[1,2,3,4,6,7,8]`. The `label` fields **and** the preset source both still encoded the correct order (Day1 Upper-Str/Bench → Day2 Lower-Str/Squat → Day3 Rest → Day4 Upper-Hyp/Incline → Day5 Lower-Hyp/Good-Morning). **Fixed in the DB** via a self-verifying service-role `tsx` script (two-phase renumber to `1..7` by label, archived day parked at 8) after a dry-run + user confirm. **The Supabase MCP can't reach the prod project (`stwmtvigdbtmmedevqip`)** — it's on a different org; use a service-role script for real-data work (memory: `supabase-prod-db-access`).
- **Hardening — PR [#113](https://github.com/Rahul-Personal-lists/workout-tracker/pull/113)** on `fix/day-order-hardening`: new pure [src/lib/day-order.ts](src/lib/day-order.ts) `planDayOrder` (live days contiguous `1..N`, auto "Day N" labels synced to position, custom labels preserved, archived parked above the live range) + [scripts/smoke-day-order.ts](scripts/smoke-day-order.ts) (6 cases). [program.ts](src/app/actions/program.ts): added `applyDayOrder` two-phase writer (mirrors `renumberExercises`, no-ops when clean); `reorderDay` now array-swaps the neighbor + normalizes — **retires the `swap_day_order` RPC** (left in the DB, unused → no migration); `addDay` inserts at a temp `day_number` then splices at a true **live position** + normalizes (removes the manual descending-shift loop). History labels read live (a past session's "Day N" follows the day's current slot; title/week/date unchanged). No "reset order" UI (a no-op once labels==position).
- **Verification:** `tsc`/`lint` (0 errors; 2 pre-existing `progression.ts` warnings)/`build` green, smoke 6/6, **plus a live end-to-end run on the test account** (Playwright + `scripts/test-otp.ts`): clicking "Move day later" fired the real `reorderDay` and the DB showed the moved day's label synced to its new position; inserting a Rest day "After Day 2" via the real `addDay` produced contiguous `[1..6]` with synced labels; test account restored after. Built brainstorm → [spec](docs/superpowers/specs/2026-06-29-day-order-hardening-design.md) → [plan](docs/superpowers/plans/2026-06-29-day-order-hardening.md) → inline execution.
