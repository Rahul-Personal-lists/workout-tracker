# Session log

(latest first; only the most recent 3 sessions are kept)

## 2026-06-05 — Program Library Stages 3–4: plan detail + graceful 2-program-cap flow

- Continued [#96](https://github.com/Rahul-Personal-lists/workout-tracker/pull/96) (Stages 0–2) on `feat/program-library-plan-detail`. Replaced the `/program/library/[programId]/[dayId]` **stub** with the real plan detail: back header + "Plan N" + a plan-tabs pill row (server `Link`s across the program's training days, `aria-current` on the active one — not a swipe carousel) + `PlanStats` row + exercise rows (`ExerciseThumb` + `MuscleBadge` via server-only `getMuscleRegionsForExercise` + sets×reps/`from {weight}`) + sticky Start CTA. Keyed by `day_number`, training days only (rest days skipped, so PLAN numbering doesn't drift). `force-dynamic` (reads user weight + program count).
- **Stage 4 — graceful max-2 flow:** `StartProgramButton` now takes `programs: ProgramSummary[]` + `presetName`. Below the cap it seeds directly (`seedPresetProgram` redirects on success; unexpected throws toast). **At the 2-program cap** it opens an archive-one bottom-sheet (built on `useDialog`, mirroring `FilterSheet`/`ConfirmSheet` shells) listing the user's non-archived programs — tapping one runs `archiveProgram` then `seedPresetProgram`, surfacing the in-progress-session guard error inline. Both the landing page and plan detail now fetch `getAllPrograms()` and pass it down (both `force-dynamic`). No new server actions — reused `seedPresetProgram` + `archiveProgram`.
- **Docs:** CLAUDE.md routes block gained the `library` subtree; `plans/program-library.md` status table + per-stage blockquotes flipped 3–4 to ✅ (Stage 0 → 🟡 — catalog expansion / Q1 is the lone remaining deferral: only the 4 existing presets are tagged, so `get_lean`/`push_pull`/`small_home` + the 2/5/6-day sections stay empty).
- **Verification:** `tsc --noEmit`, `npm run lint` (0 errors; 2 pre-existing `progression.ts` warnings), and a production build all green — all three library routes compile, `[programId]`/`[dayId]` correctly dynamic. **Gap:** this checkout has no `.env.local`/Supabase env, so the live seed→redirect, the cap→archive sheet, and visual rendering weren't exercised in-browser (same gap #96 flagged). Logic mirrors the merged `StartWorkoutButton`/`PresetList`/`program-switcher` paths.

## 2026-05-29 — Repo audit: architecture docs, diagrams, and a verified refactor pass

- **Docs/diagrams (new):** [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — full subsystem map + 7 Mermaid diagrams (auth/request flow, ER schema across all 10 tables, workout-logging sequence, route map, progression model, SW caching, onboarding state machine). [docs/CODE_AUDIT.md](docs/CODE_AUDIT.md) — prioritized findings (fixed vs deferred). README rewritten. CLAUDE.md drift corrected: `src/middleware.ts`→`src/proxy.ts` (Next 16 rename), `/today`→`/program`, `/calendar`→`/progress`, 4-tab nav, full 16-migration list, and the false "(app) layout also redirects" claim (it's proxy-only).
- **Correctness fixes:** `getTodayWeightLb` now uses the user tz (was server-local → wrong day on a UTC runtime); `editSetLog` no longer rewrites `logged_at` (was reordering the per-exercise chart when editing old sessions — shared `upsertSetLog` helper, `stampLoggedAt` flag); `body-stats` `windowSeries`/`padToWindow` unified on one `WINDOW_DAYS` map (a boundary point was kept by the filter but dropped from the axis); exercise-history top-set now breaks weight ties by reps; `archiveProgram` gained the in-progress-session guard `setActiveProgram` already had; weekly-summary email CTAs repointed `/today`→`/program`; `body-client` object-URL leak fixed (revoke effect).
- **Cleanup (net −173 lines):** removed all dead `revalidatePath("/today")` calls, `getDisplayName`, `SessionSummary`, `renameProgram`/`renameDay`, `display-name-field.tsx` + `sign-out.tsx` + `setDisplayName`, dead `units.ts` cookie helpers, `void isUnits`; extracted `renumberExercises`; program-switcher trigger now shows the active program name.
- **Lint:** was already red on `main` (11 React-19 compiler-rule errors). Fixed 3 structurally (theme-picker → server `initialTheme`, skip-undo-banner redundant setState, blank-program-form scoped-disable) and suppressed 8 valid-pattern sites (hydration gates, layout-effect DOM measurement, event-handler DOM writes) with justifications. `tsc --noEmit` and `eslint` both exit 0.
- **Security:** cron Bearer check now constant-time (`timingSafeEqual`); `/api/auth/callback` validates the `next` param (single leading slash); Recharts colors moved off hardcoded hex/lime to CSS vars.
- **Verification:** a 6-way adversarial review workflow checked every behavior-changing edit (incl. confirming, against the postgrest-js source, that omitting `logged_at` from a single-object upsert preserves it on conflict) — **0 regressions**.
- **Deferred (in CODE_AUDIT):** a11y modal focus-traps, per-screen token migration, `duration_seconds = 0` skip-sentinel collision (needs an `is_rest_skip` column), peak-taper week generalization (12-wk-only), body-client state-model cleanup, two empty/duplicate-timestamp migrations. No working-tree commit was made.

## 2026-05-28 — Workout page UX: floating timer, reachable Finish, durable reorder

- Shipped [#67](https://github.com/Rahul-Personal-lists/workout-tracker/pull/67) on `fix/workout-page-ux`: bottom nav now hides on `/workout/*` (was `z-40` and covering the `z-30` Finish bar), `RestTimerBar` got a `floating` prop so the active countdown lives inside the fixed Finish footer above the button (inline mount keeps the idle rest-duration disclosure; returns null while a timer runs), and `setExerciseOrder` merges instead of throwing when the workout's snapshot diverges from `program_exercises` (filter `orderedIds` to surviving siblings, append the rest in current order, only throw when merged list is empty).
- Workout client also got a 2.2s "Order saved" / "Couldn't save order — reverted." pill — replaced the silent `console.error`-only catch. Tiny local React state, no toast library.
- Preview verified: Finish button reachable without the nav blocking, rest timer stays pinned above Finish at every scroll position, stops cleanly. Drag-driven reorder + toast not automated (dnd-kit pointer-distance is brittle to scripted gestures); validation logic covered by typecheck + inspection.
- Caveat: `setExerciseOrder` is shared with `/program/edit` Save — that path always passes the exact live set, so the merge degenerates to "exactly orderedIds" and behavior is unchanged there.
