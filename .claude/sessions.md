# Session log

(latest first; only the most recent 3 sessions are kept)

## 2026-07-11 — Expo v1 wrap-up: Phases 3–8 shipped, Capacitor shell retired (this PR + trainr-mobile#5–#30)

- **Amend (2026-07-12): Phase 8 — custom-exercise creation — also shipped** (trainr-mobile [#28](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/28) [#29](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/29) [#30](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/30), final merge 2026-07-12, after this entry was written): the v1 de-scope reversed — full web parity for photo reframe, video crop+trim, and the in-app recorder. v1 code complete includes it.

- **The Expo rewrite is v1 code complete** (all mobile-side per the moved roadmap): Phase 3 workout core (trainr-mobile [#5](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/5) [#6](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/6) [#7](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/7)), Phase 4 program management ([#8](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/8) [#9](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/9) [#10](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/10)), Phase 5 progress + history ([#11](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/11) [#12](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/12)), Phase 6 body + settings ([#17](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/17) [#18](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/18) [#19](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/19) + the delete-account edge function HERE, [#118](https://github.com/Rahul-Personal-lists/workout-tracker/pull/118)), Phase 7 native extras + distribution ([#21](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/21) step cues as **scheduled local notifications** — absorbing the shell's rest-timer notification job; [#22](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/22) tap haptics + under-5s pulse; [#23](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/23) EAS/notification-icon/exact-alarm config + DISTRIBUTION.md; [#24](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/24) EAS project link). EAS project `trainr` (owner `trainr.01`): first production AAB + preview APK built 2026-07-11, Rahul's device pass done; **Play closed test parked** (12+ testers × 14 days; runbook in trainr-mobile `docs/DISTRIBUTION.md`).
- **delete-account deployed + verified today** (Rahul deployed; the function had been 404 since #118 merged): `scripts/verify-delete-account.ts` **10/10** on its own throwaway, plus a mobile-UI two-tap deletion E2E on a second throwaway (auth user / rows / storage asserted gone via service role; the app's post-delete `signOut()` 403s benignly — the user is already gone). Evidence: [trainr-mobile#19 comment](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/19#issuecomment-4948690897).
- **This PR retires the Phase 0 Capacitor shell**: `mobile/` (85 tracked files) + `.github/workflows/android-shell.yml` removed; `docs/MOBILE_ROADMAP.md` stub updated. Why: Phase 0 was explicitly the stopgap ("this buys time; everything below replaces it") and the Expo app now owns every job the shell had, notifications included. Recover via `git log -- mobile/`. Web app otherwise untouched — this is the sanctioned single post-final-phase docs PR (rule from Phase 2 / [#117](https://github.com/Rahul-Personal-lists/workout-tracker/pull/117)).

## 2026-07-10 — Expo rewrite Phase 2: trainr-mobile data layer ([trainr-mobile#2](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/2))

- Roadmap Phase 2 shipped in [trainr-mobile#2](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/2): **typed client + `db:types` copy flow** (types stay generated HERE, mobile's `npm run db:types` copies from the sibling checkout — no second CLI link), **TanStack Query v5** (staleTime 30 s to match #115's `staleTimes {dynamic:30}`; AppState→`focusManager` on native), **pure libs copied verbatim @ `1053c50`** (`progression`/`day-order`/`starter-program` + `tz` adapted: cookie `getUserTimezone()` → device `getDeviceTimezone()`), the **program-hub + logging read set** (13 helpers) ported from `queries.ts` (same names for diffability; no media signing until mobile Phase 4; query-key registry for Phase 3 invalidation), and a vertical slice (Program + Settings tabs render live DB data; sign-out clears the query cache).
- **Shared-invariants decision (the roadmap parked it at Phase 2): duplicate client-side, DB constraints are the enforcement, NO new Postgres RPCs.** Why: the partial unique index already rejects double-active and the demote→promote gap fails soft (`getCurrentProgram` fallback); `planned_*` snapshotting can't be an RPC without porting progression math to SQL; per-feature RPCs would couple every mobile write phase to a migration round-trip here. Existing `swap_day_order`/`resume_session` RPCs stay in use from both clients. Revisit triggers: second real user, drift incident, third client. Full reasoning: trainr-mobile `docs/superpowers/specs/2026-07-10-expo-phase2-data-layer-design.md` — **mobile specs/plans live mobile-side from Phase 2 on**.
- **`docs/MOBILE_ROADMAP.md` → stub** (this PR): the roadmap itself moved to trainr-mobile `docs/ROADMAP.md` per the Capacitor spec's earmark; phase status now updates in-repo there.
- Verification (mobile-side): tsc/lint green, expo-doctor 20/20, verbatim-copy diffs empty; **live browser E2E zero console errors/warnings** (playwright MCP this session — no puppeteer scripts needed): out-of-band claude-test session → Program tab shows **"12-Week Hypertrophy · 12 weeks · 5 days" from the live DB** → Settings shows "Test User" → cache paint on tab return → reload persistence → sign-out to /login. NOT verified: Hermes on-device (Intl tz, AsyncStorage under the provider) — Rahul's Expo Go pass on trainr-mobile#2. Built brainstorm → spec → plan → inline execution (docs in trainr-mobile).

## 2026-07-10 — Expo rewrite Phase 1: trainr-mobile scaffold ([trainr-mobile#1](https://github.com/Rahul-Personal-lists/trainr-mobile/pull/1))

- Roadmap Phase 1 shipped as a **new private repo [trainr-mobile](https://github.com/Rahul-Personal-lists/trainr-mobile)** (`~/Desktop/work/trainr-mobile`; `main` = pristine SDK 57 template, PR #1 = the work; Rahul's decisions: Phase-1-only scope, name `trainr-mobile`, private): expo-router 4-tab shell with web-parity tab bar (lucide, lime active + w-8 accent bar), ported OTP login (`shouldCreateUser:false`), supabase-js + AsyncStorage session (AppState auto-refresh), `src/theme/tokens.ts` + `tailwind.config.js` mirroring `globals.css` (NativeWind 4.2 / Tailwind 3.4), CI typecheck+lint, expo-doctor 20/20. Spec + plan committed here on `docs/expo-phase1`.
- **SDK 57 template gotchas** (template > memory, per its own AGENTS.md): ships `src/app/` + unstable `NativeTabs` (kept classic `Tabs` for styling parity); `web.output: "static"` pre-renders routes in Node so a module-scope Supabase client crashes on browser globals → **`web.output: "single"` + Platform-guarded storage**; `darkMode: "class"` so RN-web accepts the manual dark scheme; `src/types/css.d.ts` so CI's tsc passes without the gitignored expo-generated types; `expo lint` self-scaffolds eslint on first run; `pkill -f 'expo start'` self-matches the invoking shell's cmdline (use `ss -ltnp` PID kills).
- **GoTrue 400s `signInWithOtp` for example.com** — the web repo never hit it (its flow mints via `test-otp.ts` + calls `verifyOtp` directly). Mobile browser E2Es: mint → out-of-band `verifyOtp` in page context (same storage key) → app picks the session up on reload; documented in trainr CLAUDE.md. Also **`test-otp.ts` needs `.env.local`**, which had vanished (env moved to `.env` Jun 5, sans service key) — restored via dashboard paste after a Vercel dead end (this machine's CLI login `rahul0191` doesn't own the deployed project, which lives under `rahulpatidar0191s-projects`; `vercel link --yes` may have left an empty `workout-tracker` project in the `sunrgy` team).
- Verification: tsc/lint/doctor green, CI proven with and without generated types, **full live browser E2E zero console errors** (session pickup → 4 tabs → Settings shows claude-test email → reload persistence → sign-out → login) + screenshots; driven by puppeteer-core + system Chrome (no preview MCP this session). Rahul's Expo Go pass covers the real-email send path. Built brainstorm → [spec](docs/superpowers/specs/2026-07-10-expo-phase1-scaffold-design.md) → [plan](docs/superpowers/plans/2026-07-10-expo-phase1-scaffold.md) → inline execution.

